spring:
  application:
    name: payment
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      uri: http://${CONFIG_HOST:localhost}:${CONFIG_PORT:8888}
      name: payment
      enabled: false
#      fail-fast: true
#      password: ${CONFIG_PASS:dev}
#      username: user
